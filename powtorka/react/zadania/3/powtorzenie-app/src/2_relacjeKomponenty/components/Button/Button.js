import React from "react";
import classes from "./Button.module.css";

export const Button = () => {
  return <button className={classes.button}>Click me</button>;
};

const Button1 = () => {
  return <button className={classes.button1}>Click me</button>;
};

export default Button1;
